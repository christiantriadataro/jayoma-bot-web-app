export const THEME_TYPE_DARK = "Light" as const;
export const THEME_TYPE_LIGHT = "Dark" as const;
export const THEME_MESSAGE = "Theme must be within the ThemeProvider"

export const ThemeInitialState = {
    theme: THEME_TYPE_LIGHT
}

